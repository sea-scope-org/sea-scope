import { enUS } from 'date-fns/locale';

// `date-fns` locale object used by `format()`, `formatDistanceToNow()`, and
// `react-day-picker`'s `Calendar` (via the base `DatePicker` / `DateRangePicker`).
export const DATE_FNS_LOCALE = enUS;
