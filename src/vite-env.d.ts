// Vite-injected build-time constants. Values are pinned at `define` time in
// `vite.config.ts` — when this file changes, restart the dev server.

/** ISO-8601 timestamp of the last git commit on the deployed code. Used by
 *  the SEO JSON-LD as `dateModified` so AI search engines have an explicit
 *  freshness signal. */
declare const __SITE_LAST_MODIFIED__: string;
